{
  "name": "ott-web-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["api", "web-app"],
  "scripts": {
    "prepare": "husky install",
    "commit-msg": "commitlint --edit $1"
  },
  "dependencies": {
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "@commitlint/cli": "^12.1.1",
    "@commitlint/config-conventional": "^12.1.1"
  }
}
